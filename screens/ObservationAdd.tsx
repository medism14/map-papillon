import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ObservationAdd = () => {
  return (
    <View>
      <Text>ObservationAdd</Text>
    </View>
  )
}

export default ObservationAdd

const styles = StyleSheet.create({})