import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ObservationsList = () => {
  return (
    <View>
      <Text>ObservationsList</Text>
    </View>
  )
}

export default ObservationsList

const styles = StyleSheet.create({})